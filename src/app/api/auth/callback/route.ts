import { NextResponse } from "next/server";

// TODO: LinkedIn OAuth callback — Phase 4
export async function GET() {
  return NextResponse.json({ message: "LinkedIn OAuth callback — not implemented yet" });
}
